[pytest]
# Put the repo root on sys.path so `from server import ...` resolves under a
# bare `pytest` invocation (CI), matching `python -m pytest` locally.
pythonpath = .
testpaths = server/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
